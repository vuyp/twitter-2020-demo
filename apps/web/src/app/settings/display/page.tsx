import { DisplaySettingsScreen } from '@/components/screens/settings-screen';
export const metadata = { title: 'Display' };
export default function DisplayPage() {
  return <DisplaySettingsScreen />;
}
